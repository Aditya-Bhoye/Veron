# Veron

Marketing site for **VERON** — point-of-sale software for restaurants and bars.

Served by GitHub Pages from the repository root. This repo contains the static
site only: `index.html` plus its two images. The application source lives in a
separate private repository and must never be pushed here.
